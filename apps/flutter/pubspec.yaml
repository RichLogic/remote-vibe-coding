name: rvc_flutter
description: Mobile Flutter scaffold for remote-vibe-coding.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: any
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
